!Adding specifics for our h2 and spring sql framework
!to notify we already have data and framework in data.sql and schema.sql files ready.
spring.jpa.hibernate.ddl-auto-none
logging.level.org.springframework.jdbc.datasource.init.ScriptUtils=debug

